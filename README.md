# LearnLink Courses Service

Course upload, course discovery, AI quiz conversion, live classes, live quizzes, premium key points, and auto-grading endpoints.

## Run

```bash
npm install
npm run dev
```

Default port: `4200`.

